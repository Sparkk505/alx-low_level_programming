#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints all the numbers of base 16 in lowercase
 *
 * Return: Always 0 (succses)
*/

int main(void)
{
	char w = 'a';
	int m = 0;

	while (m < 16)
	{
		if (m < 10)
			putchar(n + '0');
		else
		{
			putchar(w)
			w++;
		}
		m++;
	}
	putchar('\n');
	return (0);
}

