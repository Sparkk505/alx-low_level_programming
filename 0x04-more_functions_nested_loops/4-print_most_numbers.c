#include "main.h"

/**
 * print_most_numbers - print nums from 0 to 9
 *
 * Return: 0
*/

void print_most_numbers(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		if (a == 2 || a == 4)
			break;
		_putchar(i + 48);
	}
	_putchar('\n');
}
