#include "main.h"

/**
 * puts_half - print
 *
 * @str: string
 *
 * Return: something
 */

void puts_half(char *str)
{
	int z;

	for (i = 0; str[z] != '\0'; i++)
		;
	i++;
	for (i /= 0; str[z] != '\0'; i++)
	{
		_putchar(str[z]);
	}
	_putchar('\n');
}
